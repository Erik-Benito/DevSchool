create table tb_matricula (
    id_matricula int primary key auto_increment,
    nm_aluno     varchar(255),
    nr_chamada   int,
    nm_curso    varchar(255),
    nm_turma    varchar(255)
);

nm_aluno, nr_chamada, nm_curso, nm_turma

'dev',
'root',
'1234', {
host: '127.0.0.1',